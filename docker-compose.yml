version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg15
    container_name: postgres_db
    restart: always
    environment:
      
      POSTGRES_DB: ai_doc_platform
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"   # expose to local machine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
       test: ["CMD-SHELL", "pg_isready -U postgres"]
       interval: 10s
       timeout: 5s
       retries: 5

  web:
    build: .
    container_name: ai_doc_api
    restart: always
    ports:
      - "8000:8000"
<<<<<<< HEAD
    env_file: .env
    depends_on:
      - db

  flaskapi:
    build: ./flaskapi
    container_name: flask_api
    ports:
      - "5000:5000"

  react:
    build: ./frontend
    container_name: react_app
    ports:
      - "3000:80"

  db:
    image: postgres:15
    container_name: postgres_db
    environment:
      POSTGRES_DB: user_management
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
=======
    env_file:
      - .env
    volumes:
      - .:/app/backend       
      - media_files:/app/media
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"

  # ── Zookeeper (required by Kafka) ──
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: ai_doc_zookeeper
    restart: always
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    volumes:
      - zookeeper_data:/var/lib/zookeeper/data
      - zookeeper_logs:/var/lib/zookeeper/log

  # ── Kafka ──
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: ai_doc_kafka
    restart: always
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      # PLAINTEXT_HOST = for connections from your local machine
      # PLAINTEXT = for connections from inside Docker network
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    volumes:
      - kafka_data:/var/lib/kafka/data
    depends_on:
      - zookeeper
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 15s
      timeout: 10s
      retries: 5

  # ── Kafka UI — visual dashboard ──
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: ai_doc_kafka_ui
    restart: always
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
    depends_on:
      - kafka

volumes:
  postgres_data:
  media_files:
  zookeeper_data:
  zookeeper_logs:
  kafka_data:
>>>>>>> 4dd605a (replaced my open ai with gemini api)
